# Déploiement

Dans le dossier docker, modifier le fichier .env.local avec les bonnes informations.
Renommer le fichier .env.local en .env

```bash
cd docker
```

```bash
docker compose up -d
```

## Utilisation

Connectez-vous à l'application avec l'utilisateur admin et le mot de passe admin. ()
