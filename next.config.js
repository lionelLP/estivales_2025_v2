/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'images.unsplash.com',
      'localhost',
      'estivales-brou.vercel.app'
    ],
  },
}

module.exports = nextConfig 