/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "images.unsplash.com",
      "localhost",
      "estivales-brou.vercel.app",
      "img.youtube.com",
    ],
  },
};

module.exports = nextConfig;
